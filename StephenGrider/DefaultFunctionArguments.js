// ES6
function makeAjaxRequest(url, method = 'GET') {
  // logic to make the request
}

// Coding Exercise 1 - Using Default Arguments
function sum(a = 0, b = 0) {
  return a + b;
}

// Coding Exercise 2 - Dumping Unused Code
function addOffset(style = {}) {
  style.offset = '10px';

  return style;
}
